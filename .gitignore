# ROS 2 / colcon workspace artifacts
build/
install/
log/

# Third-party source built into the workspace (cloned by setup, not ours to track)
src/kobuki_core/
src/kobuki_ros/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/

# Kinect / libfreenect firmware (downloaded locally, do not commit)
.libfreenect/
*.bin

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
