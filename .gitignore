# ROS 2 / colcon workspace artifacts
build/
install/
log/

# Third-party source built into the workspace (cloned by setup, not ours to track)
src/kobuki_core/
src/kobuki_ros/
src/ecl_tools/
src/ecl_lite/
src/ecl_core/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/

# Kinect / libfreenect firmware (downloaded locally, do not commit)
.libfreenect/
*.bin

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# ONNX hand models (downloaded by setup, ~8MB binaries)
models/
src/gesture_node/models/

# Laptop-only dev tools (webcam gesture tester + its venv) — not part of the
# colcon workspace / Pi build, do not track.
tools/
